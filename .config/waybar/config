{
  "layer": "top",
  "position": "right",
    "modules-left": ["hyprland/workspaces"],
    "modules-right": ["pulseaudio", "bluetooth", "battery", "clock"],

    "hyprland/workspaces": {
      "format": "{name}",
      "format": "{icon}",
      "tooltip": false,
      "all-outputs": true,
      "format-icons": {
        "active": "",
        "default": ""
      }
    },

    "clock": {
      "format": "{:%H:%M}",
    },

    "pulseaudio": {
      "format": "{volume}%",
      "format-muted": "M",
      "tooltip": false,
      "scroll-step": 1,
    },

    "bluetooth": {
      "format": "{status}",
      // "format-disabled": "", // an empty format will hide the module
      "format-connected": "{num_connections}",
      "tooltip-format": "{device_enumerate}",
      "tooltip-format-enumerate-connected": "{device_alias} {device_address}"
    },

    "battery": {
      "format": "{capacity}%",
      "format-charging": "<span color='#b4befe'></span> {capacity}%",
      "tooltip": false,
    },
}
